export interface WatermarkStyle {
    width: number;
    height: number;
    color?: string;
    fontSize?: number;
    fontFamily?: string;
    opacity?: number;
}

export interface WatermarkPosition {
    dx: number;
    dy: number;
  }